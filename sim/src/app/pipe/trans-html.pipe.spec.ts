import { TransHtmlPipe } from './trans-html.pipe';

describe('TransHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new TransHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
